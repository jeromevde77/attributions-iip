# Copier en .env et adapter

# JWT (générer une vraie clé en prod : openssl rand -base64 48)
JWT_SECRET=change-me-with-a-real-secret

# Port HTTP exposé (défaut 8080)
HTTP_PORT=8080

# Origine autorisée pour CORS (l'URL du frontend)
CORS_ORIGIN=http://localhost:8080
